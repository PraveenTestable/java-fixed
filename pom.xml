<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
             http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.testable</groupId>
  <artifactId>bank-whitebox</artifactId>
  <version>1.0.0</version>
  <name>Bank White-Box Metrics Demo</name>
  <description>Simple Java repo satisfying all Testable Strategy white-box metrics</description>

  <properties>
    <java.version>17</java.version>
    <maven.compiler.source>${java.version}</maven.compiler.source>
    <maven.compiler.target>${java.version}</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <!-- Tool versions -->
    <junit.version>5.10.2</junit.version>
    <jacoco.version>0.8.11</jacoco.version>
    <pmd.version>3.21.2</pmd.version>
    <checkstyle.version>3.3.1</checkstyle.version>
    <spotbugs.version>4.8.3.0</spotbugs.version>
    <pitest.version>1.15.3</pitest.version>
    <pitest.junit5.version>1.2.1</pitest.junit5.version>
    <owasp.version>9.0.9</owasp.version>

    <!-- Coverage thresholds (WB metric gates) -->
    <jacoco.instruction.min>0.80</jacoco.instruction.min>  <!-- Statement Coverage >= 80% -->
    <jacoco.branch.min>0.70</jacoco.branch.min>            <!-- Branch Coverage   >= 70% -->
    <pitest.mutation.min>70</pitest.mutation.min>           <!-- Mutation Kill Rate >= 70% -->
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.12.1</version>
      </plugin>

      <!-- JUnit 5 runner -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
      </plugin>

      <!--
        JaCoCo — Statement Coverage %, Branch Coverage %, Path Coverage proxy
        WB metrics: Statement Coverage %, Branch Coverage %, Coverage Delta %
      -->
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco.version}</version>
        <executions>
          <execution><id>prepare-agent</id><goals><goal>prepare-agent</goal></goals></execution>
          <execution>
            <id>report</id><phase>test</phase><goals><goal>report</goal></goals>
          </execution>
          <execution>
            <id>check</id><goals><goal>check</goal></goals>
            <configuration>
              <rules>
                <rule>
                  <element>BUNDLE</element>
                  <limits>
                    <limit>
                      <counter>INSTRUCTION</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>${jacoco.instruction.min}</minimum>
                    </limit>
                    <limit>
                      <counter>BRANCH</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>${jacoco.branch.min}</minimum>
                    </limit>
                  </limits>
                </rule>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!--
        PMD + CPD — Cyclomatic Complexity, Cognitive Complexity, Lint violations,
                    Code Duplication (CPD)
        WB metrics: Execution Path Integrity, Technical Debt Impact, Violation Density,
                    Structural Cleanliness Score, Multi-Point Failure Probability
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-pmd-plugin</artifactId>
        <version>${pmd.version}</version>
        <configuration>
          <failOnViolation>true</failOnViolation>
          <printFailingErrors>true</printFailingErrors>
          <minimumTokens>50</minimumTokens>
          <rulesets>
            <ruleset>/category/java/bestpractices.xml</ruleset>
            <ruleset>/category/java/design.xml</ruleset>
            <ruleset>/category/java/errorprone.xml</ruleset>
            <ruleset>/category/java/security.xml</ruleset>
          </rulesets>
        </configuration>
        <executions>
          <execution>
            <goals><goal>check</goal><goal>cpd-check</goal></goals>
          </execution>
        </executions>
      </plugin>

      <!--
        Checkstyle — Naming Conventions, Syntactic Uniformity, Violation Density
        WB metrics: Semantic Consistency Score, Syntactic Uniformity Score,
                    Violation Density per KLOC, Environment Standardization
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-checkstyle-plugin</artifactId>
        <version>${checkstyle.version}</version>
        <configuration>
          <configLocation>google_checks.xml</configLocation>
          <failsOnError>true</failsOnError>
          <consoleOutput>true</consoleOutput>
          <includeTestSourceDirectory>true</includeTestSourceDirectory>
        </configuration>
        <executions>
          <execution><goals><goal>check</goal></goals></execution>
        </executions>
      </plugin>

      <!--
        SpotBugs — SAST: injection risks, insecure patterns, auth weaknesses
        WB metrics: Exploit Surface Identification, Best Practice Compliance,
                    Entry Point Sanitization, Data Flow Security Analysis
      -->
      <plugin>
        <groupId>com.github.spotbugs</groupId>
        <artifactId>spotbugs-maven-plugin</artifactId>
        <version>${spotbugs.version}</version>
        <configuration>
          <effort>Max</effort>
          <threshold>Low</threshold>
          <failOnError>true</failOnError>
        </configuration>
        <executions>
          <execution><goals><goal>check</goal></goals></execution>
        </executions>
      </plugin>

      <!--
        PIT (PITest) — Mutation Kill Rate, Logic Error Sensitivity, Test Rigor
        WB metrics: Mutation Kill Rate % >= 70%, Weak Spot Localization,
                    Boundary Mutant Analysis, Semantic Integrity Check
      -->
      <plugin>
        <groupId>org.pitest</groupId>
        <artifactId>pitest-maven</artifactId>
        <version>${pitest.version}</version>
        <dependencies>
          <dependency>
            <groupId>org.pitest</groupId>
            <artifactId>pitest-junit5-plugin</artifactId>
            <version>${pitest.junit5.version}</version>
          </dependency>
        </dependencies>
        <configuration>
          <targetClasses><param>com.testable.bank.*</param></targetClasses>
          <targetTests><param>com.testable.bank.*</param></targetTests>
          <mutationThreshold>${pitest.mutation.min}</mutationThreshold>
          <coverageThreshold>80</coverageThreshold>
          <outputFormats><outputFormat>HTML</outputFormat><outputFormat>XML</outputFormat></outputFormats>
        </configuration>
      </plugin>

      <!--
        OWASP Dependency-Check — SCA: Known CVE Count, Supply Chain Security,
        License Compliance, Version Lag Assessment
        WB metrics: CVE Score, Supply Chain Risk, Transitive Risk Score
      -->
      <plugin>
        <groupId>org.owasp</groupId>
        <artifactId>dependency-check-maven</artifactId>
        <version>${owasp.version}</version>
        <configuration>
          <failBuildOnCVSS>7</failBuildOnCVSS>
          <format>ALL</format>
        </configuration>
        <executions>
          <execution><goals><goal>check</goal></goals></execution>
        </executions>
      </plugin>

    </plugins>
  </build>
</project>
